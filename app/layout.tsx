import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Hypnocade - Interactive Video Synthesizer Arcade Machines",
  description:
    "Transform any space into an immersive visual playground. Hypnocade machines combine familiar arcade controls with video art tools to create an interactive art experience.",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
